To use Windows Inno setup installer to create an installer, this directory needs to contain:

From http://openssl.org
* openssl.exe
* openssl.cnf

From http://nssm.cc
* nssm.exe

And VC_redist.x64.exe (for openssl and uniproxy) matching the version of Visual Studio you are using.
